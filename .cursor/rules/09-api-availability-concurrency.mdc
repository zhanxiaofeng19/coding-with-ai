---
description: 高可用高并发专项约束 — 超时/重试/熔断/限流/降级/隔离
alwaysApply: true
---

# 高可用高并发规范

## 超时控制
- 所有远程调用必须设置超时（HTTP / RPC / Redis / DB）
- 超时时间分级：
  - Redis：≤ 100ms
  - 数据库单次查询：≤ 500ms
  - 内部 RPC：≤ 1s
  - 外部 HTTP：≤ 3s
- 接口整体超时 ≤ 上游调用方超时的 80%

## 重试策略
- 重试仅限幂等操作
- 重试次数上限：2~3 次
- 重试间隔使用指数退避 + 随机抖动
- 非幂等操作失败直接返回，不重试
- 必须设置重试预算（单位时间内最大重试次数）

## 熔断规则
- 依赖服务错误率 > 50% 持续 10s 触发熔断
- 熔断后进入半开状态，放行少量请求探测恢复
- 熔断期间走降级逻辑
- 核心依赖和非核心依赖使用不同熔断器

## 限流规则
- 入口限流：API 网关层按接口 + 用户维度限流
- 服务内限流：核心写接口加限流保护
- 限流算法：令牌桶 / 滑动窗口
- 被限流后返回明确的错误码和 Retry-After

## 降级策略
- 每个外部依赖必须有降级方案
- 降级优先级：缓存兜底 > 默认值 > 功能关闭
- 降级必须可配置化（配置中心开关控制）
- 降级日志必须包含降级原因和影响范围

## 隔离机制
- 核心业务与非核心业务使用不同线程池
- 慢接口使用独立线程池，防止拖垮正常请求
- 数据库连接池按业务分组
- 不同优先级的 MQ 消费使用不同消费者组

## 热点防护
- 热点数据使用本地缓存 + 分布式缓存二级架构
- 热点写入使用合并写/异步写
- 秒杀/抢购场景必须有排队机制
- 热点 key 检测与自动打散

## 容量评估
新增接口或功能必须评估：
- 预期 QPS（日常 + 峰值）
- 依赖服务的承受能力
- 数据库/缓存/MQ 的容量是否充足
- 是否需要预扩容
