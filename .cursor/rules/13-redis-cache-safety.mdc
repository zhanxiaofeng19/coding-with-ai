---
description: Redis 缓存安全与一致性规范 — 穿透/击穿/雪崩/一致性
alwaysApply: true
---

# Redis 缓存安全规范

## 缓存穿透防护
现象：大量请求查询不存在的数据，穿透缓存直达数据库
方案：
- 缓存空值：查询结果为空时缓存空对象，TTL 设短（1~5 min）
- 布隆过滤器：在缓存前增加布隆过滤器拦截不存在的 key
- 请求校验：在入口层校验参数合法性，拦截明显非法请求

## 缓存击穿防护
现象：热点 key 过期瞬间，大量并发请求同时穿透到数据库
方案：
- 互斥锁：缓存 miss 时加分布式锁，只有一个请求回源，其余等待
- 逻辑过期：缓存不设物理 TTL，值中包含逻辑过期时间，异步刷新
- 永不过期 + 异步更新：热点数据定时刷新，不依赖 TTL

## 缓存雪崩防护
现象：大量缓存同时过期或 Redis 不可用
方案：
- TTL 加随机抖动（见 12-redis-design.mdc）
- 多级缓存：本地缓存（Caffeine）+ 分布式缓存（Redis）
- 限流降级：Redis 不可用时触发降级，限制回源并发数
- 预热：服务启动时预加载热点数据

## 缓存一致性策略

### 推荐模式：Cache Aside
- 读：先读缓存，miss 则读 DB 并回填缓存
- 写：先更新 DB，再删除缓存
- 延迟双删（可选）：更新 DB → 删缓存 → 延迟 500ms → 再删缓存

### 一致性保障
- 缓存删除失败必须有重试机制（MQ 重试 / 定时补偿）
- 核心数据设置较短 TTL 作为最终兜底
- 数据变更的顺序：先 DB 后缓存，绝不反过来

### 不推荐的模式
- Write Through / Write Behind：实现复杂，故障恢复困难
- 缓存与 DB 双写无事务保障：一致性无法保证

## 回源保护
- 缓存 miss 回源时加限流，防止回源风暴
- 使用 singleflight 模式：相同 key 的并发请求只有一个回源
- 回源超时控制 ≤ 1s
- 回源失败返回降级数据（旧缓存/默认值），不直接报错

## 慢命令禁令
生产环境禁止使用：
- KEYS *（使用 SCAN 替代）
- FLUSHALL / FLUSHDB
- 大 key 的 DEL（使用 UNLINK 异步删除）
- 复杂度 O(N) 的命令在 N > 1000 时必须评估
