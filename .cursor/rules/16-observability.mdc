---
description: 可观测性规范 — traceId、日志、指标、告警
alwaysApply: true
---

# 可观测性规范

## TraceId 透传
- 所有入口（HTTP/RPC/MQ）必须生成或接收 traceId
- traceId 通过 MDC 在线程间传递
- 异步调用（线程池/CompletableFuture）必须传递 traceId
- MQ 消息必须携带 traceId
- 跨服务调用通过 Header 传递 traceId

## 日志规范

### 格式
```
[时间] [级别] [traceId] [类名.方法名] 消息
```

### 级别使用
- ERROR：数据丢失风险、服务不可用、需要人工介入
- WARN：异常但可自动恢复、降级生效、重试成功
- INFO：关键业务节点（请求入口、状态变更、关键分支）
- DEBUG：详细调试信息（仅开发环境开启）

### 必须记录的日志
- 接口入口：方法名 + 关键参数摘要（脱敏后）
- 异常捕获：异常类型 + 消息 + 上下文（ERROR）
- 外部调用：目标服务 + 耗时 + 结果状态
- 关键分支：走了哪个分支及原因（INFO/WARN）
- 性能异常：耗时超过阈值的操作（WARN）

### 禁止事项
- 禁止在循环中打日志
- 禁止日志中包含敏感信息（密码、完整身份证、完整银行卡）
- 禁止使用 System.out/System.err
- 禁止日志格式拼接使用 + 号（使用占位符 {} ）

## 核心指标

### 必须监控的指标
- 接口 QPS / TPS
- 接口延迟（P50 / P99 / P999）
- 接口错误率
- 线程池活跃数 / 队列长度
- 连接池使用率
- JVM 内存 / GC 次数和耗时
- MQ 消费延迟 / 积压量

### 指标命名
格式：`{服务名}_{模块}_{指标类型}_{单位}`
示例：
- order_api_request_total
- order_api_request_duration_ms
- order_db_connection_pool_active

## 告警规则
- 核心接口错误率 > 1% → P1 告警
- 核心接口 P99 > 1s → P2 告警
- 线程池使用率 > 80% → P2 告警
- MQ 消息积压 > 10 万 → P2 告警
- 数据库慢查询 > 1s → P3 告警
- 连接池使用率 > 80% → P2 告警

## 告警分级
- P0：服务完全不可用 → 立即响应
- P1：核心功能异常 → 15 分钟内响应
- P2：性能劣化或非核心异常 → 1 小时内响应
- P3：预警类 → 工作时间内处理
