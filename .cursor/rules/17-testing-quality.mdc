---
description: 测试与质量规范 — 单测/集成测试/边界/压测
alwaysApply: true
---

# 测试与质量规范

## 单元测试
- 核心业务逻辑必须有单元测试
- 覆盖正常流 + 至少 2 个异常流
- 测试方法命名：should_动作_when_条件
- 使用 Mock 隔离外部依赖
- 禁止测试中依赖外部服务或数据库

## 测试覆盖要求
- Service 层核心方法覆盖率 ≥ 80%
- 工具类方法覆盖率 ≥ 90%
- Controller 层通过集成测试覆盖
- 整体行覆盖率目标 ≥ 60%

## 必须覆盖的场景
- 空值/null 输入
- 边界值（0、负数、最大值）
- 空集合
- 并发修改
- 异常抛出路径
- 幂等重复调用

## 集成测试
- 使用 Testcontainers 或 H2 模拟数据库
- Mock 外部服务（WireMock / MockServer）
- 测试接口的完整请求→响应链路
- 验证数据库状态变更

## 测试数据管理
- 测试数据在测试前构造、测试后清理
- 禁止依赖测试环境的固定数据
- 使用 Builder 模式构造测试对象

## 压测要求
- 新上线的核心接口必须压测
- 压测目标：日常 QPS 的 3~5 倍
- 压测必须在独立环境（禁止在生产压测）
- 压测报告包含：QPS、P99、错误率、资源使用率

## 质量门禁
- 编译通过
- 单测通过
- 覆盖率达标
- 静态扫描无 BLOCKER/CRITICAL 问题
