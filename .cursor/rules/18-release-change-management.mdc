---
description: 发布与变更管理规范 — 发布流程/灰度/回滚/DDL变更/配置变更
alwaysApply: true
---

# 发布与变更管理规范

## 发布流程
1. 发布计划 Review（使用 release-plan-template.md）
2. 预发环境验证
3. 灰度发布（从小流量到全量）
4. 观察期（至少 30 分钟）
5. 全量发布
6. 发布后监控确认

## 灰度策略
- 灰度维度：用户 ID 尾号 / 城市 / 渠道 / 百分比
- 灰度步骤：1% → 5% → 20% → 50% → 100%
- 每个阶段观察 ≥ 15 分钟
- 观察指标：错误率、P99、核心业务指标

## 回滚规则
- 每次发布必须有回滚方案
- 回滚条件明确（如错误率 > 5%、P99 > 2s）
- 回滚必须可一键执行
- 回滚后必须验证服务恢复正常

## DDL 变更规范
- DDL 变更独立于代码发布
- 执行顺序：先加字段/索引 → 部署新代码 → 清理旧字段
- 大表 DDL 使用 pt-online-schema-change 或 gh-ost
- DDL 在低峰期执行
- 必须有回滚 DDL 脚本

## 配置变更规范
- 配置变更通过配置中心（Nacos/Apollo），不改代码
- 配置变更有审批和记录
- 关键配置变更在低峰期执行
- 配置变更后观察 ≥ 10 分钟

## 数据修复规范
- 数据修复脚本必须有 Review
- 先在测试环境验证
- 生产执行前备份受影响数据
- 分批执行，每批后验证
- 记录修复日志（谁、什么时间、改了什么、原因）

## 禁止事项
- 禁止周五下午发布
- 禁止大促前 3 天发布非紧急变更
- 禁止跳过灰度直接全量（hotfix 除外但需审批）
- 禁止在不可回滚的情况下发布
