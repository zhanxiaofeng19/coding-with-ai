---
description: 全局工程原则 — 所有编码决策的基础约束
alwaysApply: true
---

# 核心工程原则

## 设计哲学
- 优先选择简单稳定的方案，拒绝炫技式设计
- 避免不必要的抽象层，每一层抽象必须有明确的复用或解耦理由
- 代码必须在事故压力下可维护，拒绝只有作者能理解的实现
- 永远考虑生产环境的失败场景，不写只能在 happy path 下运行的代码

## 修改原则
- 只修改需求要求的最小范围，不顺手重构不相关模块
- 保持现有公共接口不变，新增优于修改
- 保持向后兼容，字段只增不删、枚举只扩不缩
- 不改变现有业务语义，除非需求明确要求

## 高并发意识
所有代码默认假设 QPS > 2000，必须考虑：
- 线程安全与锁竞争
- 内存可见性（volatile / happens-before）
- GC 压力与对象分配频率
- 连接池耗尽风险
- 级联失败传播

必须避免：
- 热路径上的 synchronized 粗粒度锁
- 无界队列
- 循环内创建大对象
- 异步流中的阻塞 IO

## 输出要求
- 先输出可用的最终代码，再简要说明
- 解释用结构化要点，不教基础概念
- 所有面向用户的文字使用中文
