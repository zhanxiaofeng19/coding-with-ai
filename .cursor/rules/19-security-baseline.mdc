---
description: 安全基线规范 — 鉴权/脱敏/审计/输入校验/密钥管理
alwaysApply: true
---

# 安全基线规范

## 鉴权
- 所有对外接口必须经过鉴权
- 使用 Token（JWT / OAuth2）鉴权，禁止明文传输密码
- Token 有过期时间，支持刷新
- 管理后台接口与 C 端接口使用不同鉴权体系

## 授权
- 基于角色的访问控制（RBAC）
- 数据权限校验：用户只能访问自己的数据
- 接口权限校验在网关层或 AOP 层统一处理
- 最小权限原则

## 输入校验
- 所有外部输入必须校验（类型、长度、范围、格式）
- 使用参数校验注解（@Valid / @Validated）
- SQL 使用参数化查询，禁止字符串拼接
- 防止 XSS：输出编码
- 防止 CSRF：Token 校验

## 敏感数据处理
- 手机号展示：138****8888
- 身份证展示：110***********1234
- 银行卡展示：6222 **** **** 1234
- 日志中敏感字段自动脱敏
- 密码存储使用 BCrypt，禁止明文或 MD5
- 敏感数据传输使用 HTTPS

## 密钥管理
- 禁止在代码中硬编码密钥/密码
- 密钥通过配置中心或密钥管理服务注入
- 数据库密码、API Key 等使用加密配置
- 定期轮换密钥

## 审计日志
- 关键操作必须记录审计日志：登录、数据修改、权限变更、配置变更
- 审计日志包含：操作人、时间、操作类型、操作对象、变更内容
- 审计日志独立存储，不可篡改

## 接口安全
- 限流防刷：同一用户/IP 的请求频率限制
- 防重放：请求携带时间戳 + 签名
- 敏感操作二次验证（短信/密码）
- 接口不暴露内部实现细节（异常堆栈、数据库信息）
