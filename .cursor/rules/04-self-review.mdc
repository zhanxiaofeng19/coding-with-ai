---
description: 编码后自检规则 — agent 完成编码后必须执行的检查清单
alwaysApply: true
---

# 编码后自检清单

编码完成后，必须逐项检查以下清单。发现问题必须修复后才能标记任务完成。

## 正确性检查
- [ ] 所有异常路径是否都有处理（catch 后不能为空）
- [ ] 空值判断是否完整（参数、返回值、集合）
- [ ] 边界条件是否覆盖（空集合、零值、最大值、负数）
- [ ] 类型转换是否安全（Long→int 溢出、精度丢失）

## 并发安全检查
- [ ] 共享可变状态是否有线程安全保护
- [ ] 是否存在竞态条件（check-then-act）
- [ ] 分布式锁的获取和释放是否在 finally 中
- [ ] 是否有死锁风险（多锁场景的锁顺序）
- [ ] 乐观锁/CAS 是否有重试上限

## 资源管理检查
- [ ] 数据库连接/IO 流/HTTP 连接是否在 finally 或 try-with-resources 中关闭
- [ ] 是否有连接泄漏风险
- [ ] 线程池是否有界且有拒绝策略
- [ ] 定时任务是否有超时控制

## 性能检查
- [ ] 是否存在 N+1 查询
- [ ] 循环内是否有远程调用或数据库查询
- [ ] 事务范围是否最小化（事务内无远程调用）
- [ ] 大集合操作是否有分批处理
- [ ] 是否有不必要的序列化/反序列化

## 数据一致性检查
- [ ] 缓存与数据库的一致性方案是否明确
- [ ] 数据变更的顺序是否正确（先DB后缓存 / 先写后发消息）
- [ ] 幂等逻辑是否完整（唯一键/状态机/去重表）
- [ ] 并发修改是否有版本号保护

## 可观测性检查
- [ ] 关键入口是否有日志（方法入参摘要）
- [ ] 异常是否有 error 级别日志（含上下文信息）
- [ ] 关键分支是否有 warn/info 日志
- [ ] traceId 是否能贯穿完整链路
- [ ] 核心指标是否有埋点（计数/耗时/成功率）

## 兼容性检查
- [ ] 新增字段是否有默认值
- [ ] 接口返回值是否只增不改
- [ ] 枚举是否只扩不缩
- [ ] 数据库字段变更是否可以灰度（先加字段→部署新代码→再删旧字段）
- [ ] 消息体变更是否向前兼容

## 安全检查
- [ ] 用户输入是否有校验和过滤
- [ ] 敏感数据是否脱敏（日志、响应）
- [ ] 权限校验是否完整
- [ ] SQL 是否使用参数化查询
