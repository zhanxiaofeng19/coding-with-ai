---
description: 中间件韧性约束 — 连接池/线程池/超时/熔断/监控
alwaysApply: true
---

# 中间件韧性规范

## 通用规则
所有中间件连接必须遵循：
- 连接池化，禁止每次请求新建连接
- 设置连接超时和读写超时
- 配置最大连接数上限
- 连接健康检查（心跳/ping）
- 连接泄漏检测和告警

## 连接池配置原则

| 参数 | 建议值 | 说明 |
|------|-------|------|
| 最小空闲 | 核心线程数 | 保持基本连接 |
| 最大连接数 | 预估 QPS / 单连接 TPS | 不超过中间件最大连接限制 |
| 获取超时 | 1~3s | 等待连接的最大时间 |
| 空闲超时 | 10~30min | 超时回收空闲连接 |
| 最大生命周期 | 30~60min | 强制回收防止连接异常 |

## 线程池规范
- 核心业务和非核心业务使用隔离的线程池
- 禁止使用 Executors.newCachedThreadPool()（无界线程池）
- 线程池必须命名（便于排查线程 dump）
- 必须配置拒绝策略（CallerRunsPolicy 或自定义降级）
- 队列容量有界（建议 1000~5000）

## 超时配置
- 每个中间件客户端必须配置超时
- 超时时间 < 上游超时时间的 80%
- 区分连接超时（短）和读超时（按业务定）

## 熔断配置
- 外部服务调用必须配置熔断器
- 熔断阈值：错误率 > 50%，窗口期 10s
- 半开探测间隔：5~10s
- 熔断期间返回降级结果

## 背压处理
- 下游处理不过来时，上游必须有背压机制
- 方式：限流 / 排队 / 丢弃非核心请求
- MQ 消费积压时降低消费速率而非无限重试

## 资源上限
- 所有池化资源必须有最大值
- 所有队列必须有界
- 所有缓冲区必须有大小限制
- 定期检查资源使用率，>80% 触发告警
