---
description: 消息队列设计规范 — 主题命名、消息体、幂等消费、重试、死信
alwaysApply: true
---

# 消息队列设计规范

## 主题命名
格式：`{环境}_{服务名}_{业务域}_{动作}`
示例：
- prod_order_trade_created
- prod_payment_callback_received
- prod_inventory_stock_deducted

规则：
- 全部小写，下划线分隔
- 动作使用过去式（表示已发生的事件）
- 一个主题只承载一类消息

## 消息体规范
- 必须包含字段：messageId、timestamp、traceId、version、data
- messageId 全局唯一（UUID 或雪花 ID）
- version 用于消息体 schema 演进
- data 内只放业务必要字段，不放大对象
- 单条消息大小 ≤ 256KB

```json
{
  "messageId": "msg_20260402_001",
  "timestamp": 1743580800000,
  "traceId": "trace_xxx",
  "version": "1.0",
  "data": {
    "orderId": "123456",
    "status": "CREATED"
  }
}
```

## 生产者规范
- 发送超时 ≤ 3s
- 发送失败有重试（最多 3 次）
- 重试仍失败必须有兜底（写本地表 + 定时补偿）
- 事务消息：数据变更与消息发送需要原子性时使用事务消息或本地消息表
- 禁止在数据库事务内同步发送消息

## 消费者规范

### 幂等消费（强制）
- 基于 messageId 去重（去重表/Redis Set）
- 或基于业务唯一键 + 状态机判断
- 两种方式可叠加使用

### 重试策略
- 消费失败自动重试，最多 3~5 次
- 重试间隔递增：10s → 30s → 1min → 5min → 10min
- 达到重试上限后投递到死信队列

### 死信队列
- 每个消费组配置死信队列
- 死信消息必须有告警通知
- 死信消息必须有人工处理入口

### 消费限速
- 单消费者并发数可配置
- 消息堆积 > 阈值时触发告警
- 批量消费时单批大小 ≤ 32 条

## 顺序消息
- 需要顺序的场景使用顺序消息（同一个 sharding key 进同一个 queue）
- sharding key 选择业务维度（如 orderId、userId）
- 顺序消息的消费者必须单线程

## 消息积压应急
- 积压 > 10 万条触发告警
- 应急方案：临时扩容消费者 / 跳过非核心消息 / 转储后批量处理
- 禁止直接清空消息
