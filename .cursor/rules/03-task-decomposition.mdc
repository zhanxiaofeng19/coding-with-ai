---
description: 任务拆解规则 — 将需求拆分为可执行的独立任务
alwaysApply: true
---

# 任务拆解规则

## 拆解原则
- 每个任务可独立编码、独立测试、独立提交
- 任务粒度控制在 1~4 小时可完成
- 任务间依赖关系必须显式标注
- 按 数据层 → 业务层 → 接口层 → 联调层 的顺序排列

## 必须独立拆出的任务类型

### 数据变更类
- 数据库 DDL 变更（新增表/字段/索引）→ 独立 PR
- 数据迁移脚本 → 独立 PR + 回滚脚本
- Redis key 设计与预热方案

### 契约定义类
- 接口契约定义（请求/响应结构）→ 独立 PR，先于实现
- MQ 消息体定义 → 独立 PR

### 实现类
- 核心业务逻辑（正常流）
- 异常流与降级逻辑 → 不可与正常流混在一个任务中
- 幂等处理逻辑

### 质量类
- 单元测试（核心逻辑）
- 集成测试（含外部依赖 mock）
- 配置变更与开关

## 任务描述格式

每个任务必须包含：
- 任务标题（动词开头）
- 依赖的前置任务编号
- 改动范围（哪些文件/模块）
- 验收标准（怎么算做完）
- 风险点（可能的阻塞或需要确认的问题）

## 禁止事项
- 禁止一个任务包含多个不相关的改动
- 禁止把 DDL 变更和业务代码放在同一个任务中
- 禁止跳过异常流处理直接提交
