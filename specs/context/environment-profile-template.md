# 环境画像

> 本文件描述各环境的差异，帮助 agent 生成适配不同环境的配置和代码。

## 环境列表

<!-- Q: 你的项目有哪些环境？填写每个环境的配置差异。 -->

### 开发环境（dev）

| 维度 | 配置 |
|------|------|
| 部署方式 | <!-- 示例：单节点 / Docker Compose --> |
| 数据库 | <!-- 示例：本地 MySQL / 共享开发库 --> |
| Redis | <!-- 示例：单节点 Redis --> |
| MQ | <!-- 示例：本地 RocketMQ / Mock --> |
| 外部依赖 | <!-- 示例：Mock 第三方服务 --> |
| 限流 | <!-- 示例：不限流 --> |
| 日志级别 | <!-- 示例：DEBUG --> |
| 监控 | <!-- 示例：无 --> |

### 测试环境（test / staging）

| 维度 | 配置 |
|------|------|
| 部署方式 | <!-- 示例：K8s 单副本 --> |
| 数据库 | <!-- 示例：共享测试库 --> |
| Redis | <!-- 示例：共享测试 Redis --> |
| MQ | <!-- 示例：共享测试 MQ --> |
| 外部依赖 | <!-- 示例：对接测试环境 --> |
| 限流 | <!-- 示例：宽松限流 --> |
| 日志级别 | <!-- 示例：INFO --> |
| 监控 | <!-- 示例：基本监控 --> |

### 预发环境（pre）

| 维度 | 配置 |
|------|------|
| 部署方式 | <!-- 示例：K8s 双副本 --> |
| 数据库 | <!-- 示例：连生产从库（只读） --> |
| Redis | <!-- 示例：独立 Redis 集群 --> |
| MQ | <!-- 示例：独立 MQ 集群 --> |
| 外部依赖 | <!-- 示例：对接生产环境 --> |
| 限流 | <!-- 示例：生产配置 --> |
| 日志级别 | <!-- 示例：INFO --> |
| 监控 | <!-- 示例：完整监控 --> |

### 生产环境（prod）

| 维度 | 配置 |
|------|------|
| 部署方式 | <!-- 示例：K8s 多副本（≥3），多 AZ --> |
| 数据库 | <!-- 示例：主从 + 读写分离 --> |
| Redis | <!-- 示例：Redis Cluster 6 节点 --> |
| MQ | <!-- 示例：RocketMQ 4 Broker --> |
| 外部依赖 | <!-- 示例：生产环境 --> |
| 限流 | <!-- 示例：严格限流 --> |
| 日志级别 | <!-- 示例：INFO（异常时可动态调 DEBUG） --> |
| 监控 | <!-- 示例：完整监控 + 告警 --> |

## 环境间差异要点

<!-- Q: 列出环境间需要特别注意的差异。

示例：
- dev 环境无限流，生产有严格限流 → 注意代码中限流组件的配置差异
- 预发连生产从库只读 → 写操作必须有环境判断
- 生产多 AZ 部署 → 需考虑跨 AZ 延迟
- 配置中心 namespace 按环境隔离
-->

## 特性开关

<!-- Q: 你们的特性开关机制是什么？

示例：
- 使用 Nacos 配置作为特性开关
- Key 格式：feature.{功能名}.enabled = true/false
- 灰度维度：用户 ID 尾号 / 城市 / 百分比
- 管理后台：feature-admin.internal
-->
