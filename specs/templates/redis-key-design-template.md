# Redis Key 设计文档

## 修改历史

| 日期 | 修改人 | 修改内容 |
|------|--------|---------|
| YYYY-MM-DD | 姓名 | 初始版本 |

## 1. Key 清单

| Key 模式 | 数据结构 | 用途 | TTL | Value 大小估算 |
|----------|---------|------|-----|---------------|
| service:domain:type:{id} | STRING | | | |

## 2. 详细设计

### Key: `service:domain:type:{id}`

| 项目 | 内容 |
|------|------|
| 数据结构 | STRING / HASH / LIST / SET / ZSET |
| 序列化 | JSON / Protobuf |
| TTL | 基础值 + 随机抖动 |
| Value 示例 | |
| 数据来源 | <!-- DB 表名或计算逻辑 --> |
| 更新时机 | <!-- 如：写入时更新 / 定时刷新 --> |
| 缓存策略 | <!-- Cache Aside / Read Through --> |

### 写入流程

1. 
2. 
3. 

### 读取流程

1. 查询 Redis
2. 命中 → 返回
3. 未命中 → 查询 DB → 写入 Redis（加锁防击穿）→ 返回

## 3. 一致性方案

| 数据变更场景 | 缓存操作 | 失败处理 |
|------------|---------|---------|
| 创建 | <!-- 如：写DB后写缓存 --> | |
| 更新 | <!-- 如：先更新DB，再删除缓存 --> | <!-- 如：MQ 重试删除 --> |
| 删除 | <!-- 如：先删缓存，再删DB，再延迟删缓存 --> | |

## 4. 风险评估

| 风险 | 概率 | 影响 | 防护措施 |
|------|------|------|---------|
| 缓存穿透 | | | <!-- 如：缓存空值 --> |
| 缓存击穿 | | | <!-- 如：互斥锁 --> |
| 缓存雪崩 | | | <!-- 如：TTL 抖动 --> |
| 热点 key | | | <!-- 如：本地缓存 --> |
| 大 key | | | <!-- 如：拆分 --> |

## 5. 监控指标

- 缓存命中率
- Key 数量
- 内存使用
- 慢命令
