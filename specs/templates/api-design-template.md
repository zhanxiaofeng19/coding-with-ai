# API 设计文档

## 修改历史

| 日期 | 修改人 | 修改内容 |
|------|--------|---------|
| YYYY-MM-DD | 姓名 | 初始版本 |

## 1. 接口概述

| 项目 | 内容 |
|------|------|
| 接口名称 | |
| 请求方式 | GET / POST / PUT / DELETE |
| 请求路径 | /api/v1/xxx |
| 所属模块 | |
| 是否幂等 | 是 / 否 |
| 幂等机制 | <!-- 如：基于 requestId 去重 --> |
| 鉴权方式 | <!-- 如：Bearer Token --> |
| 限流策略 | <!-- 如：100 次/秒/用户 --> |

## 2. 请求参数

### Header

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| Authorization | String | 是 | Bearer Token |
| X-Request-Id | String | 是 | 请求唯一标识（幂等键） |

### Path 参数

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| | | | |

### Query 参数

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| | | | | |

### Body（JSON）

```json
{
}
```

| 字段 | 类型 | 必填 | 校验规则 | 说明 |
|------|------|------|---------|------|
| | | | | |

## 3. 响应

### 成功响应

```json
{
  "code": "SUCCESS",
  "message": "操作成功",
  "data": {},
  "traceId": "xxx"
}
```

### data 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| | | |

### 错误响应

| 错误码 | HTTP 状态码 | 说明 | 处理建议 |
|--------|-----------|------|---------|
| | | | |

## 4. 调用示例

### 请求示例

```bash
curl -X POST 'https://api.example.com/api/v1/xxx' \
  -H 'Authorization: Bearer xxx' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

### 响应示例

```json
{
  "code": "SUCCESS",
  "message": "操作成功",
  "data": {},
  "traceId": "trace_xxx"
}
```

## 5. 性能要求

| 指标 | 目标值 |
|------|--------|
| P99 延迟 | ≤ 200ms |
| QPS 上限 | |
| 超时时间 | |

## 6. 兼容性说明

<!-- 如果是修改已有接口，说明兼容策略 -->

## 7. 变更记录

| 版本 | 变更内容 | 兼容性 |
|------|---------|--------|
| v1 | 初始版本 | - |
